const options = {
  customCss: '.swagger-ui .markdown p, .swagger-ui .markdown pre, .swagger-ui .renderedMarkdown p, .swagger-ui .renderedMarkdown pre { margin: 0 auto; font-size: 14px; }'
};

module.exports = options;
